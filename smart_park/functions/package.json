{
  "name": "smartpark-functions",
  "private": true,
  "description": "SmartPark Cloud Functions: server-side PayMongo checkout and platform stats.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:rules": "firebase emulators:exec --only firestore,storage --project demo-smartpark \"node --test --test-concurrency=1 rules-test/*.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0",
    "nodemailer": "^10.0.10"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0"
  }
}
